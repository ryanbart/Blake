export default function Home() {
  return (
    <main className="mx-auto max-w-2xl px-6 py-24">
      <h1 className="text-2xl font-semibold tracking-tight">Blake</h1>
      <p className="mt-3 text-[var(--text-muted)]">
        Conversation intelligence and CRM autofill. The dashboard lands in step 9
        of Phase 1 — the schema, ingest, and analysis layers come first.
      </p>
    </main>
  );
}
